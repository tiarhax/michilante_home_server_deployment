version: "3.8"

services:
  permanent-relay:
    image: tiarhax/michilante_dynamic_rtsp_relay:latest
    network_mode: host
    env_file: permanent_relay.env

  pinger:
    image: tiarhax/michilante_pinger
    network_mode: host
    env_file: pinger.env


